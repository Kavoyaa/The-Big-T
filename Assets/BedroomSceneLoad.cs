using UnityEngine;

public class BedroomSceneLoad : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        DataManager.Instance.playerLocation = "Bedroom";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
