using UnityEngine;

public class ClassroomSceneLoad : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        DataManager.Instance.playerLocation = "ClassroomScene";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
